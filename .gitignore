# Licence private key – never commit (vendor keeps secure)
# licence-private-key.pem
# **/licence-private-key.pem
# build/tools-keys/

# Build outputs and IDE compile artifacts
build/
.gradle/
bin/
com/
*.class
